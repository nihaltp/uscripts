# AI Queue

This folder contains the modular userscript implementation for the AI Queue tools.

- `core/` holds shared helpers used by both providers.
- `providers/` holds Gemini and ChatGPT specific logic.
- `main/` holds the browser userscript entrypoints.
